# openspec

> OpenSpec は、コードを書く前に、利用者とコーディングエージェントが共有してレビューできる計画を提供します。このファイルは CLI をインストールし、プロジェクトをセットアップします。

OpenSpec をインストールし、私のプロジェクトをセットアップしてください。以下の手順を順番に進め、停止するよう指示された箇所では停止してください。

目的：OpenSpec CLI をグローバルにインストールし、利用者のプロジェクトを初期化する。

完了条件：

- `openspec --version`でバージョン番号が表示される
- 利用者が確認したプロジェクトで、`openspec init`により`openspec/`フォルダが作成または更新されている
- 利用者が AI コーディングツールで OpenSpec を呼び出す正確な表記を案内されている

TODO:

- [ ] Node.js の実行環境を確認する
- [ ] 利用者の確認を得て、CLI をグローバルにインストールする
- [ ] `openspec`が PATH に含まれていることを確認する
- [ ] セットアップするフォルダを確認し、そこで`openspec init --tools <ids>`を実行する
- [ ] init が実際に作成した内容と呼び出し方法を報告する

## 1. 実行環境

`node --version`を実行します。OpenSpec には Node.js 20.19.0 以降が必要です。Node.js がない場合や
バージョンが古い場合は、その旨を伝えて停止してください。ただし、利用者が次に取る手順も示します。
https://nodejs.org を案内するか、nvm などのバージョンマネージャーを使用している場合は、
新しいバージョンへ切り替えるコマンドを示してください。Node.js のインストール、バージョンの切り替え、
利用者のバージョンマネージャーの再設定を自分で行わないでください。

## 2. インストール

最初に`openspec --version`を実行します。すでにバージョンが表示される場合、CLI はインストール済みです。
その旨を伝え、以下のインストールコマンドで更新するか確認してください。利用者が更新しない場合は、
ステップ 4 へ進みます。

PATH に含まれているパッケージマネージャーを使用し、npm を優先します。

```bash
npm install -g @ayumuwall/openspec@latest
pnpm add -g @ayumuwall/openspec@latest
bun add --global @ayumuwall/openspec@latest
yarn global add @ayumuwall/openspec@latest   # Yarn 1.x のみ
```

プロジェクトのロックファイルを基準に選ばないでください。グローバルインストールは、このリポジトリ自身の
依存関係をインストールする方法とは無関係です。4 つのパッケージマネージャーがどれも利用できない場合は、
その旨を伝えて停止し、別の方法を即興で試さないでください。Nix の場合は、代わりに
https://openspec.dev/docs/installation の Nix 節を案内してください。

実行前に正確なコマンドを示し、確認を得てください。この操作はプロジェクト外へソフトウェアを
インストールするため、利用者が別のパッケージマネージャーで管理したい場合があります。

インストールに sudo または管理者権限が必要な場合、権限エラーで失敗した場合、グローバル bin
ディレクトリが存在しない、または未設定と報告された場合は、停止して改めて確認してください。
シェルの起動ファイル（.bashrc、.zshrc、.profile、fish、PowerShell プロファイル）は決して編集せず、
それらを編集するセットアップコマンドも実行しないでください。必要な変更を示し、利用者自身に実行してもらいます。

## 3. PATH

`openspec --version`を実行します。コマンドが見つからない場合、このシェルの PATH にだけ含まれていない
可能性があります。パッケージマネージャーのインストール先と、利用者のシェルと OS でそのディレクトリを
PATH へ追加する方法を示し、利用者が確認するまで停止してください。インストール直後に報告されたものより
古いバージョンが表示された場合は、以前のコピーが PATH 上で優先されています。続行せず、両方の
バージョンを報告してください。利用者がバージョンマネージャーを使っている場合は、PATH を変更して
回避せず、その影響を説明してください。nvm または fnm では、CLI はインストール時に有効だった
Node.js バージョンに紐付きます。asdf または volta では、shim の再生成が必要な場合があります。

## 4. 初期化

`openspec/`を配置する場所を判断し、自由回答の質問をするのではなく、最適と思われる候補を先に示してください。
通常は、利用者が作業中のプロジェクトルートが適切です。選んだフォルダ名を示し、利用者が訂正できるようにします。
たとえば「現在の場所は ~/code/acme-api なので、ここに OpenSpec をセットアップします」と伝えます。
現在のディレクトリよりバージョン管理のルートを優先してください。モノレポでは、選んだパッケージと
その理由を伝えます。現在のフォルダ以外を対象にする場合は、`openspec init <path> --tools <ids>`のように
パスを渡します。

init は指定先に`openspec/`を作成し、その場所が誤っていても警告しません。対象がホームディレクトリ、
一時ディレクトリ、またはプロジェクトがまったくない場所の場合は停止し、プロジェクトの場所を確認してください。

次に、利用者が使用する AI コーディングツールを判断します。ここでも自由回答の質問から始めず、推測した
候補を先に示してください。あなたはおそらくそのいずれかのツール上で動作しているため、そのツール名を示し、
ほかに使用するツールがあるか確認します。Claude Code、Cursor、Copilot、Codex など、一般的な候補も
いくつか提示してください。回答によって何が変わるかも説明します。指定した各ツールには、プロジェクト内に
専用のスキルファイルとコマンドファイルが作成されます。後から init を再実行して追加できるため、現時点では
短い一覧で問題ありません。各ツールを`openspec init --help`にある ID へ対応付けます。Copilot は
`github-copilot`、Zoo Code は`roocode`です。`--tools`にはカンマ区切りの一覧を渡すため、使用する
ツールをすべて指定します。

`openspec init --tools <ids>`は、旧バージョンの OpenSpec が残したファイルを確認なしで自動的に削除します。
ホームディレクトリ内の`opsx-*.md`プロンプトも対象です。Codex では`~/.codex/prompts`にあります。
実行前に、`.../commands/openspec/`フォルダ、CLAUDE.md や AGENTS.md などのファイル内にある
OpenSpec のマーカーブロック、ホームディレクトリ内の`opsx-*.md`プロンプトを探してください。
見つかったものをすべて列挙し、init が削除することを明確に伝え、実行の許可を待ちます。何も見つからない
場合はその旨を伝え、確認を求めずに続行します。既存の`openspec/`フォルダは問題ではありません。
init はフォルダを更新しますが、仕様と変更提案には手を加えません。

その後、`openspec init --tools <ids>`を実行します。対象が現在のフォルダではない場合はパスも追加します。

## 5. 報告

作成されるはずの内容を推測せず、init が実際に表示した内容を伝えてください。作成したスキルやコマンドの
数と場所、設定ファイルの行、「Setup required」という注意事項、再起動または再読み込みが必要な対象を
報告します。一部のツールはスキル専用で、コマンドファイルが 0 件でも正常です。そのため、コマンドがない
ことだけを失敗と判断しないでください。何も生成されなかったと init が報告した場合は、再試行せず、
提案された解決方法を伝えます。

最後に、利用者のツールで OpenSpec を呼び出す方法を伝えます。要約行ではなく、init が作成したファイルから
正確な表記を取得してください。句読記号はツールごとに異なり、一部では`/opsx:propose`、ほかでは
`/opsx-propose`、Amazon Q では`@opsx-propose`を使用します。コマンドではなくスキルを受け取る
ツールでは、スキル名で呼び出します。`/openspec-propose`、Codex では`$openspec-propose`、
Kimi Code では`/skill:openspec-propose`です。

今すぐ実行：上記の TODO を完了し、OpenSpec CLI のインストール、このプロジェクトの初期化、
利用者が AI コーディングツールで OpenSpec を呼び出す正確な方法の案内を達成してください。

エージェント向けの追加情報：https://openspec.dev/llms.txt
