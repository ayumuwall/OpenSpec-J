# トラブルシューティング

具体的な問題に対する具体的な対処をまとめています。各項目では、症状、考えられる原因、解決策を説明します。ここで見つからない場合は [FAQ](faq.md) も確認してください。[Discord](https://discord.gg/YctCnvvshC) でも相談できます。

## インストールとセットアップ

### `openspec: command not found`

CLI がインストールされていないか、シェルが CLI を見つけられません。グローバルにインストールして以下を確認します。

```bash
npm install -g @ayumuwall/openspec@latest
openspec --version
```

インストールしても見つからない場合は、グローバル npm bin ディレクトリが `PATH` にない可能性があります。`npm bin -g` を実行してグローバルバイナリの場所を確認し、そのパスがシェルプロファイルに含まれていることを確認します。

### 「Node.js 20.19.0 以降が必要」

OpenSpec は Node.js 20.19.0 以降で動作します。バージョンを確認し、必要に応じてアップグレードします。

```bash
node --version
```

bun で OpenSpec をインストールする場合でも、OpenSpec 自体は Node.js 上で実行されます。そのため、`PATH` 上で Node.js 20.19.0 以降を利用できる必要があります。詳しくは [インストール](installation.md) を参照してください。

### `openspec init` が AI ツールを構成しませんでした

`init` は、どのツールをセットアップするかを尋ねます。ツールをスキップした場合や別のツールを追加したい場合は、再実行するか、非対話形式を使います。

```bash
openspec init --tools claude,cursor
```

ツール ID の完全な一覧は [サポートされているツール](supported-tools.md) にあります。すべてを対象にするには `--tools all`、ツールのセットアップをスキップするには `--tools none` を使います。

<a id="commands-dont-show-up"></a>

## コマンドが表示されない

`/opsx:propose` (または同等のツール) が表示されない場合、または何も実行しない場合は、このリストを調べてください。チェックが早い順に並べられます。

1. **入力場所が違う可能性があります。** スラッシュコマンドは、ターミナルではなく AI アシスタントのチャットに入力します。シェルに `/opsx:propose` と入力しているなら、それが原因です。[コマンドの仕組み](how-commands-work.md) を参照してください。

2. **ファイルを再生成します。** プロジェクト ルートから:

```bash
openspec update
```

これにより、設定済みツールのスキルファイルとコマンドファイルが再生成されます。

3. **アシスタントを再起動します。** ほとんどのツールは起動時にスキルとコマンドをスキャンします。新しいウィンドウを開くだけで反映されることもあります。

4. **ファイルが存在することを確認します。** Claude Code の場合、`.claude/skills/` に `openspec-*` フォルダーがあるか確認します。他のツールはそれぞれ別のディレクトリを使います。詳しくは [サポートされているツール](supported-tools.md) を参照してください。

5. **このプロジェクトが初期化されていることを確認してください。** スキルはプロジェクトごとに書き込まれます。リポジトリのクローンを作成した場合、またはフォルダーを切り替えた場合は、そこで `openspec init` (または `openspec update`) を実行します。

6. **使っているツールがコマンドファイルに対応しているか確認します。** Kimi CLI、ForgeCode、Mistral Vibe など一部のツールでは、`opsx-*` コマンドファイルは生成されません。代わりにスキルベースの呼び出しを使います。形式はツールごとに異なります。[サポートされているツール](supported-tools.md) と [コマンドの仕組み](how-commands-work.md#slash-command-syntax-by-tool) を参照してください。

## 変更の処理

### 「変更が見つかりません」

コマンドは、どの変更を意味するのかを判断できませんでした。明示的に名前を付けるか、存在するものを確認します。

```bash
openspec list                    # アクティブな変更を表示
/opsx:apply add-dark-mode        # チャットで変更名を指定
```

また、正しいプロジェクトディレクトリにいることも確認してください。

### 「アーティファクトの準備ができていません」

すべてのアーティファクトは、すでに作成されているか、依存関係を待ってブロックされています。何がブロックしているのかを確認してください。

```bash
openspec status --change <name>
```

次に、不足している依存関係を先に作成します。順序は、提案が仕様と設計を可能にし、仕様と設計がそろうとタスクを作成できる、という流れです。

### `openspec validate` が警告またはエラーを報告します

検証では、構造上の問題がないか仕様と変更をチェックします。メッセージを読みます。ファイルと問題の名前が示されています。

```bash
openspec validate <name>           # 1 件を検証
openspec validate --all            # すべて検証
openspec validate --all --strict   # より厳密に検証（CI 向け）
```

一般的な原因は、必須セクションの欠落（シナリオのない仕様など）または不正なデルタヘッダーです。ファイルを修正して再実行してください。[CLI リファレンス](cli.md#openspec-validate) には出力形式が記載されています。

### AI が不完全または間違ったアーティファクトを作成した

AI に十分なコンテキストが渡っていなかった可能性があります。次の対策が有効です。

- `openspec/config.yaml` にプロジェクトコンテキストを追加し、スタックや規約がすべてのリクエストへ挿入されるようにします。詳しくは [カスタマイズ](customization.md#project-configuration) を参照してください。
- たとえば仕様にのみ適用されるガイダンスとして、アーティファクトごとに `rules:` を追加します。
- 提案するときは、より詳細な説明を入力してください。
- すべてを一度に作る `/opsx:ff` ではなく、拡張コマンドの `/opsx:continue` を使って、アーティファクトを 1 つずつ作成し、それぞれ確認します。

### アーカイブが完了しない、または未完了のタスクについて警告が表示される

アーカイブは未完了のタスクを *ブロック*しませんが、アーカイブは通常、作業が完了したことを意味するため、警告が表示されます。タスクが意図的に残っている場合 (部分的な変更を提出している場合)、続行します。それ以外の場合は、最初にタスクを完了してください。まだ同期していない場合、アーカイブは仕様差分をメイン仕様に同期することも提案します。断る理由がない限り、「はい」と言ってください。

## 構成

### 私の `config.yaml` は適用されません

よくある原因は 3 つです。

1. **ファイル名が間違っています。** `openspec/config.yml` ではなく、`openspec/config.yaml` である必要があります。
2. **無効な YAML。** 任意の YAML バリデーターで確認します。CLI も構文エラーを行番号付きで報告します。
3. **再起動が必要だと思っている。** 再起動は不要です。設定変更はすぐ有効になります。

### "ルール内の不明なアーティファクト ID: X"

`rules:` の下のキーは、スキーマ内のどのアーティファクトとも一致しません。デフォルトの `spec-driven` スキーマの場合、有効な ID は `proposal`、`specs`、`design`、`tasks` です。スキーマの ID を確認するには:

```bash
openspec schemas --json
```

### 「コンテキストが大きすぎます」

`context:` フィールドはすべてのリクエストに挿入されるため、意図的に 50 KB に制限されています。要約するか、貼り付ける代わりに長いドキュメントにリンクしてください。コンテキストを絞るほど、結果の品質と速度が安定します。

### 「スキーマが見つかりません」

参照したスキーマ名は存在しません。利用可能なものをリストし、スペルを確認します。

```bash
openspec schemas                    # 利用可能なスキーマを一覧表示
openspec schema which <name>        # スキーマの解決元を表示
openspec schema init <name>         # カスタムスキーマを作成
```

詳しくは [カスタマイズ](customization.md#custom-schemas) を参照してください。

## 従来のワークフローからの移行

### 「非対話モードでレガシー ファイルが検出されました」

CI または非対話型シェルを使用しており、OpenSpec はクリーンアップすべき古いファイルを見つけましたが、プロンプトを表示できません。自動的に承認します:

```bash
openspec init --force
```

### 移行後にコマンドが表示されない

IDE を再起動します。多くのツールは起動時にスキルを検出します。それでも表示されない場合は `openspec update` を実行し、[サポートされているツール](supported-tools.md) でファイルの場所を確認してください。

### 古い `project.md` は移行されませんでした

これは意図した挙動です。OpenSpec は `project.md` を自動削除しません。そこに作成済みの重要なコンテキストが残っている可能性があるためです。有用な部分を `config.yaml` の `context:` に移してから、必要に応じて自分で削除してください。[移行ガイド](migration-guide.md#migrating-projectmd-to-configyaml) では、抽出を AI に依頼するためのプロンプトも含めて説明しています。

## まだ行き詰まっていますか?

- **Discord:** [discord.gg/YctCnvvshC](https://discord.gg/YctCnvvshC)
- **GitHub Issues:** [github.com/Fission-AI/OpenSpec/issues](https://github.com/Fission-AI/OpenSpec/issues)
- **端末から:** `openspec feedback "問題の内容"` が Issue を作成します。

問題を報告するときは、OpenSpec バージョン（`openspec --version`）、Node バージョン（`node --version`）、AI ツール、実行した正確なコマンドと出力を含めてください。対応が速くなります。
