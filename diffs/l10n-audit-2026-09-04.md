# 翻訳棚卸し（2026-09-04）

## 対象

- 起点: `v1.0.0`
- 比較終点: `upstream-v1.11.0`
- A: v1.0.0 以降に upstream で変更された `src/**/*.ts`（178 ファイル）
- B: `diffs/upstream-v1.10.0__upstream-v1.11.0.scope.md` で `[x]` の `src/**/*.ts`（27 ファイル）
- A - B: 151 ファイル（現行ツリーに存在する 149 ファイルを検査。削除済みの 2 ファイルは対象なし）

削除済み:

- `src/core/command-generation/adapters/codex.ts`
- `src/core/command-generation/adapters/windsurf.ts`

## 翻訳対応済み

### `src/commands/completion.ts`

- シェル補完の説明文 `schema` を `スキーマ` に変更

### `src/commands/config.ts`

- 見出し `Profile settings:` を `プロファイル設定:` に変更
- `profile` の注記を `(明示指定)` / `(既定値)` に変更（設定キー `profile` は維持）
- `delivery` の注記を `(明示指定)` / `(既定値)` に変更（設定キー `delivery` は維持）
- `workflows` の注記を `(core プロファイル由来)` / `(明示指定)` / `(なし)` に変更（設定キーと値は維持）

### `src/core/openspec-root.ts`

- ユーザー向けエラー `${relativePath}/ exists but is not a directory.` を日本語化

### `src/telemetry/index.ts`

- 初回表示の telemetry 通知 `Note: OpenSpec collects anonymous usage stats. ...` を日本語化

## 翻訳対象外と判断した候補

- `src/commands/store.ts`: `Git` とコマンド例。製品名、コマンド名、フラグ、パスのため維持
- `src/commands/validate.ts`: `change/<id>`。JSON の `type: change` と対応する検索可能な識別子のため維持
- `src/commands/workflow/instructions.ts`: XML タグ、属性名、状態値。機械可読な出力形式のため維持
- `src/core/openers.ts`: `buildLaunchCommand` の内部不変条件を検査する開発者向けエラーのため維持
- `src/core/view.ts`: 表示対象となるコマンド名のため維持
- `src/prompts/searchable-multi-select.ts`: キー名 `Backspace` / `Enter` のため維持
- `src/utils/interactive.ts`: `@inquirer` 互換の失敗を模倣する内部エラー値のため維持

## 検出条件

TypeScript 構文木を使い、指定対象の文字列リテラルとテンプレートリテラルから「日本語を含まず、英字が3文字以上連続する」ものを抽出した。変数参照や式だけの引数は文字列リテラルではないため候補に含めていない。

確認対象:

- `console.log` / `console.error` / `console.warn`
- `throw new Error`
- `ora(...)`
- `spinner.text = ...`
- `spinner.fail(...)` / `spinner.succeed(...)` / `spinner.start(...)` / `spinner.warn(...)`
- `message:`
- `chalk.*(...)`
