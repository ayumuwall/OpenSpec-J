# l10n audit 2026-04-22

対象: v1.0.0 以降に upstream で変更された `src/**/*.ts` のうち、直近追従 scope の逐一確認対象外になりやすい既存 workflow テンプレートを中心に確認。

検出方法:

- `src/core/templates/workflows/**` と今回変更対象の `src/commands/workflow/**` / `src/core/**` から、ユーザー向け出力例・テンプレート説明に残る英語見出しや固定文言を `rg` で検索。
- コマンド名、フラグ名、識別子、ファイルパス、サンプルの要件名などは翻訳対象外として除外。

## 対応済み

- `src/core/templates/workflows/archive-change.ts`
  - `Change` / `Schema` / `Archived to` / `Specs` / `Warnings` / `Options` / `Target` を日本語化。
- `src/core/templates/workflows/bulk-archive-change.ts`
  - 一括アーカイブの成功・一部成功・変更なし・競合解決例の英語出力例を日本語化。
- `src/core/templates/workflows/onboard.ts`
  - apply / archive フェーズの英語出力例を日本語化。
- `src/core/templates/workflows/verify-change.ts`
  - 検証レポートのサマリー表に残っていた英語見出しを日本語化。
- `src/core/parsers/spec-structure.ts`
  - メイン仕様構造エラーの新規メッセージを日本語化。
- `src/utils/file-system.ts`
  - 一時テストファイル掃除失敗時の debug 文言を日本語化。

## 未対応候補

なし。
