# 概念

> 2 種類のアーティファクトと、現在の仕様に対する差分を変更で表す仕組みを説明します。

<!-- 骨組み：見出しのみ。3 回目のレビュー（重複排除）で範囲を絞った。
クイックスタートのアーカイブ手順が、このページで扱う予定だったループの仕組みを引き継いだため、ループのセクションは削除した。
ループの説明はクイックスタートに集約し、基本ワークフローとオプションワークフローの違いは `customize/profiles.md` へ移した。
「固定部分と変更可能な部分」のセクションも削除した。`customize/overview.md` が 2026-08-14 のレビューでその枠組みを廃止し、変更できないものを説明せず、選択肢から始める構成になったためである。
このページだけが担うのは、アーティファクトの説明。Specs では「仕様とは何か」を説明し、Changes ではデルタの概念を扱う。
`docs/concepts.md` の中核だった ADDED、MODIFIED、REMOVED のデルタ例を 1 つ使い、現在の仕様との差分として変更を示す。
ファイル形式の規則は `reference/schemas/spec-driven/index.md` の Delta specs セクションに置く。
ディスク上のパスは独立した構成セクションにせず、対応する概念の説明内に記載する。Specs では `specs/`、Changes では `changes/` を示す。
最後に、ループを説明するクイックスタートと、カスタマイズ方法を説明する `customize/overview.md` へのリンクを置く。
ブランチや PR との関係でいつアーカイブするかは `guides/teams.md` で扱う。 -->

## 仕様：実装されたシステム

## 変更：仕様差分をまとめたフォルダ
