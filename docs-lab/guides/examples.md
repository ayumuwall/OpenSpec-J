# 変更例

> 実際の草案、レビューで見つかった問題、その修正から、よい変更提案の形を学びます。

<!-- 骨組み：見出しのみ。2026-08-11 に保留し、README の索引と同期設定から除外した。
作り込んだ架空例は、この製品の使い方について誤った印象を与える。実利用から得たアーカイブ済み変更を掲載できるまで保留する。
再開時は、弱い例とレビュー済み例の組み合わせを 4 件と、アーカイブ済み変更のギャラリーを掲載する。
以前の判断も維持する。2 回目のレビューで例の比較だけに絞り、適用方法は `guides/apply.md` へ移した。 -->

## ドキュメントがないコードベースへの機能追加

## クライアントを壊さない API の変更

## 振る舞いを維持するリファクタリング

## ロールバック可能なデータベース移行

## 実際にアーカイブされた変更
