# チームでの利用

> チームで OpenSpec を運用する方法として、コミットするもの、変更と PR の関係、アーカイブする時期を説明します。

<!-- 骨組み：見出しのみ。2026-08-11 に「Teams & parallel changes」から改題した。
このページの役割は、チームでの OpenSpec 運用を説明すること。並行する変更は、同じ仕様を変更する場合のセクションだけを残した。
個人利用も含む並行変更全般の説明は、担当ページがない項目として `message-map.md` に残している。
レビュー時に範囲を広げ、Git と PR の規約をこのページで扱うことにした。クイックスタートのアーカイブ手順では仕組みを、このページでは実行時期を説明する。
追加した 3 セクションの参照元は `docs/team-workflow.md`。
「1 つの変更を 1 つのブランチと PR で扱う」では、変更フォルダとコードを同じブランチに置き、PR で計画と差分を一緒に示す。
「プルリクエストでレビューする」では、proposal、仕様デルタ、コードの順にレビューする。方針への異論は 300 行の差分ではなく proposal に記録する。
「アーカイブする時期」では、PR のマージ後にアーカイブする方法（推奨。リリース済みの作業だけが `specs/` へ反映される）と、PR 内でアーカイブする方法（単純だが差分が増える）を説明する。どちらかに決め、常に同じ方法を使う。
実装前に計画をレビューする proposal-first PR は、応用で扱う。 -->

## openspec/ を Git へコミットする

## 1 つの変更を 1 つのブランチと PR で扱う

## プルリクエストでレビューする

## アーカイブする時期

## 同じ仕様を変更する並行作業

## 応用：大規模チーム向けの規約
