# Cloudflare Pagesのリダイレクト（静的アセットより先に評価される）。
# このデプロイのルートはドキュメント専用。ランディングページは別の
# リポジトリとデプロイで管理する。
/ /docs 302
# 一時対応（2026-08-21）: Overviewの書き直し中はドキュメント索引ページがない
#（docs.sync.config.mjsを参照）。Overviewを再公開したらこの行を削除する。
/docs /docs/installation 302
