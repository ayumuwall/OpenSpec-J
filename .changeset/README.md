このディレクトリは Changesets によって管理されています。

- ローカルで changeset を追加する: `pnpm changeset`
- CI "Release (prepare)" ワークフローが Version Packages PR を作成・更新します。
- パブリッシュは GitHub Release から "Publish to npm" ワークフロー経由で行われます。
